import 'package:flutter/material.dart';

class UpdateOngoingJourneyDialog extends StatelessWidget {
  const UpdateOngoingJourneyDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
